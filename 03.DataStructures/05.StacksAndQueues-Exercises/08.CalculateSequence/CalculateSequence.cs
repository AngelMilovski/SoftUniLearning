using System;
using System.Collections.Generic;

public class CalculateSequence
{
    public static void Main()
    {
        








        //int n = int.Parse(Console.ReadLine());
        //Queue<int> queue = new Queue<int>();
        //queue.Enqueue(n);
        //var numsInDistanceFifty = new List<int>();
        //int count = 0;

        //while (queue.Count > 0 && numsInDistanceFifty.Count < 50)
        //{
        //    var current = queue.Dequeue();
        //    numsInDistanceFifty.Add(current);
        //    count++;
        //    queue.Enqueue(current + 1);
        //    queue.Enqueue((current * 2) + 1);
        //    queue.Enqueue(current + 2);
        //}
        //var result = string.Join(", ", numsInDistanceFifty);
        //Console.WriteLine(result);


    }
}